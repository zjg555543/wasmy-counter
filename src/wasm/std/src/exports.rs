//! exports exposes the public wasm API
//!
//! interface_version_8, allocate and deallocate turn into Wasm exports
//! as soon as cosmwasm_std is `use`d in the contract, even privately.
//!
//! `do_execute`, `do_instantiate`, `do_migrate`, `do_query`, `do_reply`
//! and `do_sudo` should be wrapped with a extern "C" entry point including
//! the contract-specific function pointer. This is done via the `#[entry_point]`
//! macro attribute from cosmwasm-derive.
use std::marker::PhantomData;
use std::vec::Vec;

use serde::de::DeserializeOwned;

use crate::deps::OwnedDeps;
#[cfg(feature = "stargate")]
use crate::ibc::{
    IbcBasicResponse, IbcChannelCloseMsg, IbcChannelConnectMsg, IbcChannelOpenMsg,
    IbcChannelOpenResponse, IbcPacketAckMsg, IbcPacketReceiveMsg, IbcPacketTimeoutMsg,
    IbcReceiveResponse,
};
use crate::imports::addr_validate;
use crate::imports::{ExternalApi, ExternalQuerier, ExternalStorage};
use crate::memory::build_region;
use crate::memory::{alloc, consume_region, release_buffer, Region};
#[cfg(feature = "abort")]
use crate::panic::install_panic_handler;
use crate::query::CustomQuery;
use crate::results::{ContractResult, QueryResponse, Reply, Response};
use crate::serde::{from_slice, to_vec};
use crate::types::Env;
use crate::StdError;
use crate::{CustomMsg, Deps, DepsMut, MessageInfo};
use serde::ser::StdError as OtherStdError;

#[cfg(feature = "iterator")]
#[no_mangle]
extern "C" fn requires_iterator() -> () {}

#[cfg(feature = "staking")]
#[no_mangle]
extern "C" fn requires_staking() -> () {}

#[cfg(feature = "stargate")]
#[no_mangle]
extern "C" fn requires_stargate() -> () {}

/// interface_version_* exports mark which Wasm VM interface level this contract is compiled for.
/// They can be checked by cosmwasm_vm.
/// Update this whenever the Wasm VM interface breaks.
#[no_mangle]
extern "C" fn interface_version_8() -> () {}

/// allocate reserves the given number of bytes in wasm memory and returns a pointer
/// to a Region defining this data. This space is managed by the calling process
/// and should be accompanied by a corresponding deallocate
#[no_mangle]
extern "C" fn allocate(size: usize) -> u32 {
    let input = "ex1h0j8x0v9hs4eq6ppgamemfyu4vuvp2sl0q9p3v";

    let input_bytes = input.as_bytes();
    let source = build_region(input_bytes);
    let source_ptr = &*source as *const Region as u32;

    let result = unsafe { addr_validate(source_ptr) };

    alloc(size) as u32
}

/// deallocate expects a pointer to a Region created with allocate.
/// It will free both the Region and the memory referenced by the Region.
#[no_mangle]
extern "C" fn deallocate(pointer: u32) {
    // auto-drop Region on function end
    let _ = unsafe { consume_region(pointer as *mut Region) };
}

// TODO: replace with https://doc.rust-lang.org/std/ops/trait.Try.html once stabilized
macro_rules! r#try_into_contract_result {
    ($expr:expr) => {
        match $expr {
            Ok(val) => val,
            Err(err) => {
                return ContractResult::Err(err.to_string());
            }
        }
    };
    ($expr:expr,) => {
        $crate::try_into_contract_result!($expr)
    };
}

/// This should be wrapped in an external "C" export, containing a contract-specific function as an argument.
///
/// - `Q`: custom query type (see QueryRequest)
/// - `M`: message type for request
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
pub fn do_instantiate<Q, M, C, E>(
    instantiate_fn: &dyn Fn(DepsMut<Q>, Env, MessageInfo, M) -> Result<Response<C>, E>,
    env_ptr: u32,
    info_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_instantiate(
        instantiate_fn,
        env_ptr as *mut Region,
        info_ptr as *mut Region,
        msg_ptr as *mut Region,
    );
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_execute should be wrapped in an external "C" export, containing a contract-specific function as arg
///
/// - `Q`: custom query type (see QueryRequest)
/// - `M`: message type for request
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
pub fn do_execute<Q, M, C, E>(
    execute_fn: &dyn Fn(DepsMut<Q>, Env, MessageInfo, M) -> Result<Response<C>, E>,
    env_ptr: u32,
    info_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_execute(
        execute_fn,
        env_ptr as *mut Region,
        info_ptr as *mut Region,
        msg_ptr as *mut Region,
    );
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_migrate should be wrapped in an external "C" export, containing a contract-specific function as arg
///
/// - `Q`: custom query type (see QueryRequest)
/// - `M`: message type for request
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
pub fn do_migrate<Q, M, C, E>(
    migrate_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_migrate(migrate_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_sudo should be wrapped in an external "C" export, containing a contract-specific function as arg
///
/// - `Q`: custom query type (see QueryRequest)
/// - `M`: message type for request
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
pub fn do_sudo<Q, M, C, E>(
    sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

pub fn do_sudo1<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo10<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo11<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo12<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo13<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo14<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo15<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo16<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo17<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo18<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo19<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo20<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo21<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo22<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo23<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo24<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo25<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo26<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo27<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo28<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo29<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo30<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo31<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo32<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo33<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo34<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo35<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo36<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo37<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo38<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo39<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo40<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo41<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo42<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo43<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo44<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo45<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo46<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo47<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo48<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo49<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo50<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo51<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo52<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo53<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo54<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo55<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo56<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo57<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo58<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo59<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo60<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo61<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo62<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo63<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo64<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo65<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo66<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo67<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo68<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo69<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo70<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo71<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo72<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo73<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo74<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo75<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo76<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo77<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo78<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo79<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo80<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo81<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo82<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo83<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo84<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo85<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo86<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo87<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo88<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo89<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo90<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo91<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo92<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo93<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo94<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo95<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo96<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo97<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo98<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo99<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    pub fn do_sudo999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}

    pub fn do_sudo1001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo1999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo2999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo3999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo4999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo5999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo6999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo7999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8144<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8145<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8146<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8147<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8148<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8149<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8150<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8151<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8152<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8153<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8154<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8155<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8156<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8157<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8158<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8159<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8160<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8161<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8162<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8163<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8164<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8165<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8166<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8167<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8168<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8169<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8170<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8171<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8172<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8173<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8174<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8175<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8176<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8177<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8178<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8179<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8180<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8181<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8182<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8183<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8184<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8185<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8186<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8187<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8188<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8189<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8190<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8191<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8192<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8193<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8194<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8195<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8196<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8197<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8198<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8199<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8200<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8201<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8202<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8203<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8204<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8205<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8206<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8207<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8208<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8209<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8210<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8211<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8212<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8213<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8214<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8215<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8216<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8217<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8218<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8219<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8220<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8221<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8222<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8223<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8224<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8225<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8226<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8227<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8228<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8229<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8230<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8231<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8232<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8233<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8234<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8235<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8236<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8237<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8238<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8239<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8240<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8241<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8242<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8243<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8244<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8245<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8246<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8247<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8248<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8249<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8250<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8251<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8252<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8253<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8254<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8255<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8256<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8257<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8258<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8259<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8260<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8261<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8262<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8263<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8264<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8265<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8266<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8267<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8268<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8269<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8270<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8271<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8272<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8273<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8274<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8275<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8276<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8277<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8278<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8279<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8280<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8281<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8282<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8283<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8284<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8285<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8286<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8287<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8288<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8289<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8290<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8291<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8292<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8293<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8294<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8295<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8296<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8297<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8298<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8299<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8300<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8301<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8302<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8303<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8304<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8305<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8306<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8307<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8308<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8309<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8310<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8311<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8312<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8313<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8314<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8315<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8316<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8317<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8318<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8319<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8320<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8321<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8322<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8323<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8324<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8325<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8326<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8327<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8328<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8329<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8330<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8331<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8332<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8333<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8334<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8335<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8336<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8337<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8338<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8339<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8340<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8341<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8342<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8343<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8344<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8345<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8346<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8347<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8348<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8349<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8350<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8351<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8352<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8353<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8354<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8355<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8356<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8357<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8358<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8359<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8360<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8361<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8362<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8363<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8364<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8365<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8366<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8367<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8368<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8369<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8370<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8371<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8372<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8373<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8374<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8375<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8376<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8377<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8378<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8379<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8380<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8381<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8382<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8383<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8384<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8385<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8386<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8387<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8388<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8389<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8390<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8391<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8392<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8393<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8394<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8395<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8396<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8397<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8398<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8399<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8400<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8401<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8402<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8403<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8404<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8405<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8406<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8407<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8408<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8409<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8410<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8411<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8412<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8413<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8414<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8415<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8416<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8417<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8418<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8419<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8420<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8421<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8422<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8423<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8424<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8425<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8426<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8427<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8428<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8429<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8430<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8431<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8432<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8433<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8434<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8435<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8436<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8437<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8438<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8439<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8440<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8441<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8442<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8443<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8444<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8445<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8446<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8447<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8448<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8449<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8450<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8451<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8452<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8453<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8454<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8455<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8456<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8457<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8458<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8459<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8460<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8461<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8462<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8463<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8464<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8465<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8466<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8467<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8468<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8469<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8470<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8471<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8472<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8473<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8474<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8475<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8476<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8477<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8478<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8479<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8480<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8481<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8482<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8483<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8484<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8485<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8486<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8487<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8488<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8489<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8490<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8491<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8492<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8493<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8494<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8495<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8496<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8497<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8498<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8499<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8500<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8501<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8502<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8503<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8504<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8505<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8506<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8507<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8508<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8509<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8510<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8511<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8512<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8513<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8514<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8515<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8516<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8517<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8518<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8519<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8520<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8521<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8522<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8523<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8524<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8525<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8526<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8527<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8528<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8529<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8530<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8531<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8532<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8533<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8534<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8535<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8536<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8537<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8538<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8539<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8540<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8541<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8542<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8543<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8544<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8545<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8546<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8547<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8548<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8549<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8550<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8551<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8552<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8553<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8554<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8555<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8556<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8557<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8558<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8559<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8560<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8561<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8562<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8563<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8564<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8565<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8566<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8567<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8568<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8569<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8570<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8571<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8572<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8573<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8574<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8575<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8576<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8577<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8578<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8579<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8580<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8581<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8582<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8583<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8584<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8585<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8586<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8587<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8588<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8589<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8590<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8591<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8592<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8593<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8594<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8595<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8596<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8597<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8598<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8599<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8600<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8601<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8602<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8603<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8604<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8605<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8606<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8607<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8608<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8609<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8610<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8611<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8612<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8613<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8614<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8615<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8616<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8617<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8618<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8619<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8620<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8621<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8622<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8623<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8624<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8625<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8626<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8627<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8628<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8629<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8630<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8631<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8632<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8633<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8634<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8635<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8636<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8637<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8638<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8639<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8640<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8641<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8642<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8643<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8644<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8645<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8646<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8647<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8648<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8649<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8650<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8651<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8652<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8653<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8654<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8655<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8656<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8657<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8658<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8659<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8660<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8661<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8662<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8663<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8664<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8665<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8666<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8667<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8668<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8669<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8670<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8671<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8672<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8673<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8674<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8675<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8676<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8677<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8678<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8679<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8680<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8681<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8682<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8683<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8684<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8685<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8686<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8687<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8688<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8689<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8690<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8691<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8692<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8693<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8694<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8695<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8696<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8697<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8698<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8699<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8700<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8701<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8702<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8703<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8704<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8705<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8706<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8707<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8708<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8709<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8710<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8711<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8712<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8713<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8714<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8715<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8716<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8717<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8718<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8719<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8720<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8721<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8722<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8723<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8724<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8725<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8726<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8727<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8728<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8729<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8730<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8731<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8732<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8733<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8734<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8735<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8736<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8737<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8738<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8739<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8740<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8741<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8742<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8743<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8744<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8745<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8746<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8747<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8748<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8749<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8750<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8751<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8752<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8753<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8754<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8755<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8756<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8757<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8758<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8759<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8760<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8761<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8762<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8763<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8764<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8765<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8766<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8767<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8768<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8769<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8770<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8771<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8772<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8773<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8774<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8775<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8776<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8777<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8778<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8779<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8780<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8781<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8782<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8783<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8784<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8785<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8786<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8787<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8788<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8789<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8790<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8791<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8792<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8793<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8794<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8795<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8796<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8797<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8798<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8799<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8800<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8801<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8802<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8803<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8804<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8805<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8806<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8807<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8808<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8809<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8810<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8811<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8812<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8813<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8814<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8815<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8816<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8817<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8818<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8819<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8820<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8821<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8822<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8823<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8824<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8825<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8826<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8827<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8828<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8829<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8830<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8831<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8832<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8833<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8834<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8835<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8836<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8837<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8838<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8839<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8840<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8841<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8842<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8843<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8844<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8845<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8846<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8847<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8848<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8849<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8850<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8851<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8852<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8853<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8854<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8855<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8856<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8857<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8858<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8859<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8860<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8861<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8862<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8863<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8864<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8865<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8866<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8867<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8868<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8869<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8870<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8871<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8872<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8873<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8874<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8875<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8876<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8877<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8878<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8879<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8880<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8881<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8882<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8883<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8884<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8885<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8886<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8887<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8888<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8889<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8890<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8891<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8892<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8893<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8894<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8895<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8896<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8897<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8898<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8899<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8900<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8901<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8902<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8903<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo8999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9000<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9001<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9002<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9003<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9004<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9005<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9006<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9007<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9008<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9009<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9010<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9011<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9012<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9013<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9014<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9015<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9016<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9017<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9018<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9019<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9020<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9021<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9022<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9023<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9024<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9025<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9026<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9027<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9028<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9029<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9030<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9031<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9032<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9033<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9034<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9035<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9036<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9037<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9038<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9039<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9040<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9041<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9042<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9043<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9044<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9045<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9046<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9047<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9048<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9049<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9050<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9051<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9052<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9053<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9054<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9055<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9056<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9057<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9058<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9059<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9060<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9061<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9062<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9063<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9064<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9065<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9066<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9067<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9068<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9069<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9070<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9071<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9072<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9073<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9074<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9075<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9076<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9077<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9078<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9079<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9080<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9081<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9082<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9083<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9084<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9085<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9086<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9087<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9088<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9089<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9090<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9091<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9092<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9093<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9094<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9095<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9096<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9097<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9098<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9099<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9100<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9101<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9102<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9103<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9104<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9105<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9106<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9107<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9108<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9109<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9110<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9111<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9112<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9113<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9114<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9115<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9116<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9117<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9118<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9119<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9120<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9121<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9122<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9123<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9124<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9125<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9126<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9127<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9128<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9129<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9130<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9131<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9132<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9133<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9134<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9135<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9136<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9137<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9138<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9139<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9140<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9141<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9142<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9143<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo
Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9904<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9905<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9906<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9907<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9908<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9909<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9910<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9911<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9912<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9913<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9914<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9915<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9916<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9917<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9918<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9919<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9920<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9921<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9922<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9923<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9924<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9925<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9926<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9927<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9928<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9929<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9930<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9931<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9932<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9933<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9934<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9935<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9936<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9937<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9938<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9939<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9940<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9941<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9942<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9943<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9944<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9945<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9946<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9947<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9948<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9949<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9950<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9951<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9952<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9953<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9954<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9955<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9956<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9957<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9958<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9959<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9960<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9961<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9962<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9963<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9964<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9965<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9966<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9967<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9968<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9969<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9970<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9971<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9972<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9973<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9974<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9975<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9976<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9977<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9978<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9979<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9980<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9981<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9982<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9983<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9984<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9985<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9986<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9987<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9988<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9989<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9990<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9991<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9992<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9993<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9994<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9995<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9996<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9997<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9998<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
pub fn do_sudo9999<Q, M, C, E>(sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>, env_ptr: u32,msg_ptr: u32,) -> u32 where Q: CustomQuery,M: DeserializeOwned,C: CustomMsg,E: ToString,{#[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_sudo(sudo_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32}
    

/// do_reply should be wrapped in an external "C" export, containing a contract-specific function as arg
/// message body is always `SubcallResult`
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
pub fn do_reply<Q, C, E>(
    reply_fn: &dyn Fn(DepsMut<Q>, Env, Reply) -> Result<Response<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_reply(reply_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_query should be wrapped in an external "C" export, containing a contract-specific function as arg
///
/// - `Q`: custom query type (see QueryRequest)
/// - `M`: message type for request
/// - `E`: error type for responses
pub fn do_query<Q, M, E>(
    query_fn: &dyn Fn(Deps<Q>, Env, M) -> Result<QueryResponse, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    M: DeserializeOwned,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_query(query_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_channel_open is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn does the protocol version negotiation during channel handshake phase
///
/// - `Q`: custom query type (see QueryRequest)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_channel_open<Q, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelOpenMsg) -> Result<IbcChannelOpenResponse, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_channel_open(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_channel_connect is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn is a callback when a IBC channel is established (after both sides agree in open)
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_channel_connect<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelConnectMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_channel_connect(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_channel_close is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn is a callback when a IBC channel belonging to this contract is closed
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_channel_close<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelCloseMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_channel_close(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_packet_receive is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn is called when this chain receives an IBC Packet on a channel belonging
/// to this contract
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_packet_receive<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketReceiveMsg) -> Result<IbcReceiveResponse<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_packet_receive(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_packet_ack is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn is called when this chain receives an IBC Acknowledgement for a packet
/// that this contract previously sent
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_packet_ack<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketAckMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_packet_ack(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

/// do_ibc_packet_timeout is designed for use with #[entry_point] to make a "C" extern
///
/// contract_fn is called when a packet that this contract previously sent has provably
/// timedout and will never be relayed to the calling chain. This generally behaves
/// like ick_ack_fn upon an acknowledgement containing an error.
///
/// - `Q`: custom query type (see QueryRequest)
/// - `C`: custom response message type (see CosmosMsg)
/// - `E`: error type for responses
#[cfg(feature = "stargate")]
pub fn do_ibc_packet_timeout<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketTimeoutMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: u32,
    msg_ptr: u32,
) -> u32
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    #[cfg(feature = "abort")]
    install_panic_handler();
    let res = _do_ibc_packet_timeout(contract_fn, env_ptr as *mut Region, msg_ptr as *mut Region);
    let v = to_vec(&res).unwrap();
    release_buffer(v) as u32
}

fn _do_instantiate<Q, M, C, E>(
    instantiate_fn: &dyn Fn(DepsMut<Q>, Env, MessageInfo, M) -> Result<Response<C>, E>,
    env_ptr: *mut Region,
    info_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<Response<C>>
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let info: Vec<u8> = unsafe { consume_region(info_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let info: MessageInfo = try_into_contract_result!(from_slice(&info));
    let msg: M = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    instantiate_fn(deps.as_mut(), env, info, msg).into()
}

fn _do_execute<Q, M, C, E>(
    execute_fn: &dyn Fn(DepsMut<Q>, Env, MessageInfo, M) -> Result<Response<C>, E>,
    env_ptr: *mut Region,
    info_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<Response<C>>
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let info: Vec<u8> = unsafe { consume_region(info_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let info: MessageInfo = try_into_contract_result!(from_slice(&info));
    let msg: M = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    execute_fn(deps.as_mut(), env, info, msg).into()
}

fn _do_migrate<Q, M, C, E>(
    migrate_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<Response<C>>
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: M = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    migrate_fn(deps.as_mut(), env, msg).into()
}

fn _do_sudo<Q, M, C, E>(
    sudo_fn: &dyn Fn(DepsMut<Q>, Env, M) -> Result<Response<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<Response<C>>
where
    Q: CustomQuery,
    M: DeserializeOwned,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: M = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    sudo_fn(deps.as_mut(), env, msg).into()
}

fn _do_reply<Q, C, E>(
    reply_fn: &dyn Fn(DepsMut<Q>, Env, Reply) -> Result<Response<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<Response<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: Reply = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    reply_fn(deps.as_mut(), env, msg).into()
}

fn _do_query<Q, M, E>(
    query_fn: &dyn Fn(Deps<Q>, Env, M) -> Result<QueryResponse, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<QueryResponse>
where
    Q: CustomQuery,
    M: DeserializeOwned,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: M = try_into_contract_result!(from_slice(&msg));

    let deps = make_dependencies();
    query_fn(deps.as_ref(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_channel_open<Q, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelOpenMsg) -> Result<IbcChannelOpenResponse, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcChannelOpenResponse>
where
    Q: CustomQuery,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcChannelOpenMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_channel_connect<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelConnectMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcBasicResponse<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcChannelConnectMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_channel_close<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcChannelCloseMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcBasicResponse<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcChannelCloseMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_packet_receive<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketReceiveMsg) -> Result<IbcReceiveResponse<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcReceiveResponse<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcPacketReceiveMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_packet_ack<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketAckMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcBasicResponse<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcPacketAckMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

#[cfg(feature = "stargate")]
fn _do_ibc_packet_timeout<Q, C, E>(
    contract_fn: &dyn Fn(DepsMut<Q>, Env, IbcPacketTimeoutMsg) -> Result<IbcBasicResponse<C>, E>,
    env_ptr: *mut Region,
    msg_ptr: *mut Region,
) -> ContractResult<IbcBasicResponse<C>>
where
    Q: CustomQuery,
    C: CustomMsg,
    E: ToString,
{
    let env: Vec<u8> = unsafe { consume_region(env_ptr) };
    let msg: Vec<u8> = unsafe { consume_region(msg_ptr) };

    let env: Env = try_into_contract_result!(from_slice(&env));
    let msg: IbcPacketTimeoutMsg = try_into_contract_result!(from_slice(&msg));

    let mut deps = make_dependencies();
    contract_fn(deps.as_mut(), env, msg).into()
}

/// Makes all bridges to external dependencies (i.e. Wasm imports) that are injected by the VM
pub(crate) fn make_dependencies<Q>() -> OwnedDeps<ExternalStorage, ExternalApi, ExternalQuerier, Q>
where
    Q: CustomQuery,
{
    OwnedDeps {
        storage: ExternalStorage::new(),
        api: ExternalApi::new(),
        querier: ExternalQuerier::new(),
        custom_query_type: PhantomData,
    }
}
